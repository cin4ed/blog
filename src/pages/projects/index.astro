---
import Layout from "../../layouts/Layout.astro";

const projects = [
    {
        title: "Worky",
        description:
            "A productivity tool that lets you have multiple workspaces for your macOS desktop.",
        link: "#",
    },
    {
        title: "imhearing",
        description:
            "A fun app that updates your X bio to show what you're listening to on Spotify.",
        link: "#",
    },
];
---

<Layout title="Betweenframes - Projects">
    <main class="max-w-4xl mx-auto px-4">
        <div class="flex justify-between items-center mt-5">
            <h1
                class="font-[Gruppo] text-[39px] tracking-[.25rem] leading-[25px] transition-all hover:tracking-normal"
            >
                Projects
            </h1>
            <div class="flex items-end divide-x">
                <a href="/" class="font-[Homenaje] px-2 text-xl hover:underline border-secondary-foreground hover:bg-secondary-foreground transition-all cursor-pointer ease-out duration-700"
                    >home</a
                >
                <a
                    href="/blog"
                    class="font-[Homenaje] px-2 text-xl hover:underline border-secondary-foreground hover:bg-secondary-foreground transition-all cursor-pointer ease-out duration-700"
                    >blog</a
                >
            </div>
        </div>
        <p class="mt-7 text-zinc-300">
            This is a collection of projects I believe are worth sharing. Some
            are small and simple, while others are more complex, but each one
            has its own unique story.
        </p>
        <div class="mt-4 divide-y border-foreground">
            {
                projects.map((project) => (
                    <div class="flex flex-col p-2 border-secondary-foreground hover:bg-secondary-foreground transition-all cursor-pointer ease-out duration-700 select-none">
                        <div class="font-[Homenaje] text-xl">
                            {project.title}
                        </div>
                        <div class="font-[Homenaje] text-lg text-zinc-300">
                            {project.description}
                        </div>
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
