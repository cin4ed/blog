---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<button
  class:list={[
    "flex items-center gap-2 rounded-lg bg-stone-100 px-3 py-1.5 text-sm transition-colors hover:bg-stone-200",
    className,
  ]}
>
  <slot />
</button>
